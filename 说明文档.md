# 智能起重机寻路系统技术文档 (V9)

## 1. 项目概述 (Project Overview)

本项目是一个专为工业起重机（天车/龙门吊）设计的 **2.5D 运动规划与可视化系统**。它能够在一个包含静态和动态障碍物的车间环境中，自动计算出一条从起点到终点的安全、平滑且符合机械运动学特性的路径。

### 核心能力
- **数字孪生环境**：实时维护车间地图状态，支持动态添加/移除障碍物。
- **安全避障**：基于配置空间（C-Space）理论，自动根据起重机尺寸膨胀障碍物。
- **混合寻路算法**：结合全局 A* 搜索、贪婪视线优化（Shortcut）和贝塞尔曲线平滑（Bezier Smoothing）。
- **实时可视化**：基于 Flask + Socket.IO + HTML5 Canvas 的低延迟前端交互。

---

## 2. 系统架构 (System Architecture)

系统采用分层架构设计，各层职责单一且清晰。

```mermaid
graph TD
    Client[Web Client (Canvas/JS)] <-->|Socket.IO| Server[Flask Server (app.py)]
    Server <--> Service[业务服务层 (crane_service.py)]
    Service <--> MapMgr[地图管理器 (WorkshopMapManager)]
    Service <--> Planner[智能规划器 (IntelligentCranePlanner)]
    
    subgraph 核心算法层 (smart_crane_pathfinding.py)
        MapMgr -- 提供膨胀网格 --> AStar[A* 寻路算法]
        Planner -- 原始路径 --> Optimizer[路径优化器 (path_optimizer.py)]
    end
```

---

## 3. 模块详细说明 (Module Details)

### 3.1 全局配置 (`config.py`)
- **职责**：集中管理所有硬编码参数，支持通过环境变量 (`.env`) 覆盖，适应不同部署环境。
- **关键参数**：
    - `MAP_RESOLUTION_M`: 网格分辨率（米/格），决定了寻路的精度和计算量。
    - `CRANE_FOOTPRINT_M`: 起重机安全足迹直径，直接影响障碍物膨胀半径。
    - `ENABLE_BEZIER_SMOOTHING`: 算法功能开关。

### 3.2 Web 接入层 (`app.py`)
- **职责**：系统的入口，负责 HTTP 页面渲染和 WebSocket 事件分发。
- **技术栈**：Flask, Flask-SocketIO, Eventlet (异步并发)。
- **核心事件**：
    - `connect/disconnect`: 客户端连接管理。
    - `request_path`: 接收起点终点，返回计算后的路径。
    - `add_obstacle / remove_obstacle_near`: 处理地图编辑指令并广播状态更新。

### 3.3 业务服务层 (`crane_service.py`)
- **职责**：作为 Web 层与算法层之间的桥梁，维护全局唯一的单例状态。
- **核心类 `CraneService`**：
    - 持有 `WorkshopMapManager` 实例（地图状态）。
    - 持有 `IntelligentCranePlanner` 实例（规划逻辑）。
    - 负责捕获底层算法异常，向前端返回人类可读的错误信息。

### 3.4 核心算法层 (`smart_crane_pathfinding.py`)
这是系统的“大脑”，包含两大部分：

#### A. 地图管理器 (`WorkshopMapManager`)
- **线程安全**：使用 `threading.RLock` 保护障碍物列表，支持高并发读写。
- **配置空间生成 (C-Space)**：
    - **原理**：为了将有体积的起重机简化为质点寻路，系统会将障碍物向外“膨胀”一个起重机半径。
    - **性能优化**：检测到 `scipy` 库时，自动启用 **EDT (欧几里得距离变换)** 算法，将膨胀计算性能提升数倍。若无依赖，自动回退到 Python 原生算法。
- **缓存机制**：针对不同的安全半径缓存膨胀后的网格，避免重复计算。

#### B. 智能规划器 (`IntelligentCranePlanner`)
- **2.5D 动作合成**：
    1. **投影**：将 3D 起终点投影到 2D 地面网格。
    2. **寻路**：在 2D 膨胀网格上计算路径。
    3. **提升**：生成的路径点 Z 轴高度统一设为 `safe_travel_z_m`（安全巡航高度）。
    4. **动作序列**：`起点 -> 垂直上升 -> 平面巡航 -> 垂直下降 -> 终点`。

### 3.5 路径优化器 (`path_optimizer.py`)
负责对 A* 生成的原始路径进行后处理，使其更符合机械运动特性。

#### A. 贪婪捷径优化器 (`GreedyShortcutOptimizer`)
- **目的**：消除 A* 算法产生的“锯齿”和不必要的中间节点。
- **算法**：Line-of-Sight (视线检查)。尝试直接连接路径上不相邻的两个点，如果中间无障碍，则跳过中间所有节点。
- **实现细节**：使用**高密度线性插值**进行视线碰撞检测，防止穿模。

#### B. 贝塞尔平滑器 (`BezierCurveSmoother`)
- **目的**：将折线拐角转化为平滑的圆弧，减少起重机加减速冲击。
- **核心算法**：二阶贝塞尔曲线 (Quadratic Bezier)。
- **自适应回退 (Adaptive Fallback)**：
    - 默认尝试使用最大平滑度（大圆弧）。
    - 如果大圆弧检测到碰撞（使用高密度采样），则自动减小半径重试。
    - 如果最小半径仍碰撞，则保留尖角，确保安全性优先于平滑性。

---

## 4. 核心算法原理图解

### 4.1 障碍物膨胀 (Configuration Space)
```
原始地图:       膨胀后(机器人视角):
[0, 0, 0]       [0, 1, 0]
[0, 1, 0]  -->  [1, 1, 1]  <-- 只要不碰到这些1，
[0, 0, 0]       [0, 1, 0]      实际机器人就不会撞墙
```

### 4.2 A* 启发式函数 (Heuristic)
由于起重机可以做八方向移动（含对角线），我们使用 **Octile Distance** 而非曼哈顿距离：
$$ h(n) = (\max(\Delta x, \Delta y) - \min(\Delta x, \Delta y)) + 1.414 \times \min(\Delta x, \Delta y) $$

### 4.3 贝塞尔曲线生成
对于路径段 $P_0 \to P_1 \to P_2$，在 $P_1$ 处生成曲线：
$$ B(t) = (1-t)^2 Q_0 + 2t(1-t) P_1 + t^2 Q_2 $$
其中 $Q_0, Q_2$ 是 $P_1$ 前后的控制点。

---

## 5. 数据接口定义 (Socket.IO API)

### 5.1 客户端 -> 服务端

**1. 请求路径规划 (`request_path`)**
```json
{
  "start": {"x": 2.0, "y": 2.0},
  "end":   {"x": 30.0, "y": 15.0}
}
```

**2. 添加障碍物 (`add_obstacle`)**
```json
{
  "type": "static",  // 或 "dynamic"
  "x": 10.0, "y": 5.0,
  "w": 2.0,  "h": 2.0
}
```

### 5.2 服务端 -> 客户端

**1. 地图状态更新 (`update_map_state`)**
```json
{
  "width_m": 100.0,
  "static_obstacles": {"id1": {...}},
  "dynamic_obstacles": {"id2": {...}},
  "crane_footprint_m": 5.0
}
```

**2. 路径更新 (`update_path`)**
```json
[
  [2.0, 2.0, 0.0],   // 起点
  [2.0, 2.0, 8.0],   // 上升到安全高度
  [15.0, 10.0, 8.0], // 中间点
  ...
]
```

---

## 6. 部署与运行

### 6.1 依赖安装
```bash
pip install -r requirements.txt
# 推荐安装 scipy 以获得最佳性能，但非必须
pip install scipy
```

### 6.2 启动服务
```bash
python app.py
```
服务默认运行在 `http://127.0.0.1:5000`。

### 6.3 环境变量 (`.env` 可选)
可以在项目根目录创建 `.env` 文件来覆盖默认配置：
```ini
LOG_LEVEL=INFO
MAP_WIDTH_M=150.0
CRANE_FOOTPRINT_M=3.0
ENABLE_BEZIER_SMOOTHING=true
```